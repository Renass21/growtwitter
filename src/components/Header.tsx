import styled from "styled-components";

const HeaderStyled = styled.div`
    color: blue;
`

export const Header = () => {
    return(
        <HeaderStyled>
            <p>Teste</p>
        </HeaderStyled>
    )
}